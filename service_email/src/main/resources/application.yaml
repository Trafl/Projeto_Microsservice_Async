spring:
  application:
    name: service-email

  datasource:
    url: jdbc:mysql://localhost:3306/empiricus_email?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: root

#    config:
#      import: file:.env[.properties]

server:
  port: 0

  eureka:
    client:
      service-url:
        defaultZone: http://localhost:8761/eureka
      register-with-eureka: true
      fetch-registry: true

